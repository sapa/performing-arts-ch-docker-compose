#### DOCKER IMAGE VERSIONS ####

# project/deployment name
# needs to be unique i.e. will be used as container and network name as well as for the vhost entry
COMPOSE_PROJECT_NAME=prod

# base host name that is used for the nginx virtual host directive
# should not be required to change until you change the hostname
HOST_NAME=performing-arts.ch

# need to be customized only when Let's Encrypt is used. See https://letsencrypt.org/docs/expiration-emails/
LETSENCRYPT_EMAIL=birk.weiberg@sapa.swiss

#### RUNTIME PLATFORM ENVIORNMENT PARAMS ####

# can be used to pass specific platform parameter that will override any parameter set in the config files
METAPHACTORY_OPTS=-Dconfig.global.homePage=sapa:Preview -Dconfig.environment.securityConfigStorageId=performing-arts-ch -Dconfig.environment.platformBaseIri=http://data.performing-arts.ch -Dconfig.environment.pathsToRewrite=/a/,/w/,/o/,/p/,/r/ -Dconfig.environment.shiroAuthenticationFilter=authcBasic,anon,authc -Dconfig.ui.deploymentTitle=performing-arts.ch -Dlog4j.configurationFile=file:///var/lib/jetty/webapps/etc/log4j2.xml

#### DOCKER IMAGE VERSIONS ####

BLAZEGRAPH_IMAGE=docker.metaphacts.com/snapshot/blazegraph:2.2.0-20160908.003514-6

METAPHACTORY_IMAGE=metaphacts/researchspace:3.1.0

# changes to the data image will trigger a re-creation of the current data image mounted
# i.e. you may loose you current runtime changes you have made to any configuration (namespaces, templates etc)
# METAPHACTORY_DATA_IMAGE=docker.metaphacts.com/release/platform-data:2.3.2

# CRON schedule for backups. Leave empty if not backups should be performed.
# Please note that no automatic clean-ups will be performed.
# Example: "* * * * *" - every minute | "0 0 * * *" - every night 00:00 | "15 0 * * 0" - every sunday 00:15 Am
BACKUP_CRON_SCHEDULE=0 0 * * *

# Number of most recent backup archives to keep. 
# If not specified, no backups will be deleted.
BACKUP_RETENTION_NUMBER=10

##### DO NOT CHANGE BELOW ###

# path to main docker-compose file, should not be required to change
COMPOSE_FILE=../../metaphactory/docker-compose.yml:../docker-compose.yml:./docker-compose.overwrite.yml

